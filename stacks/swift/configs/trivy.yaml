format: json
output: trivy-results.json
severity:
  - CRITICAL
  - HIGH
  - MEDIUM
vuln-type:
  - library
scanners:
  - vuln
  - misconfig
exit-code: 1
ignore-unfixed: true
timeout: 10m
cache-dir: .trivy-cache
db:
  skip-update: false
swift:
  package-manager: swift
  scan-dependencies: true
